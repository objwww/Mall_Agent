# 工具清单验收说明

调用 `GET /internal/v1/tools`，使用 `Authorization: Bearer <AGENT_CONTROL_API_KEY>`。

响应包含发布版本、SHA-256 清单摘要和当前进程实际装配的工具。工具增删或契约信息改变会生成不同摘要；清单只读，工具代码仍随受控部署发布，不能绕过审批在线替换。

请求和响应日志记录 TraceId、版本、摘要与数量，不记录工具调用中的业务敏感正文。实际调用结果继续由 Agent Run/Tool Call 协议从领域事件上报。
