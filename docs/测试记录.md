# MallAgent 首次导入测试记录

## 本次范围

- 独立运行时生产源码与自动化测试导入
- CaseLab 造题模型和 MallAgent 处理模型启动隔离
- 工单理解、证据、推理、建议、审批、执行、验证、恢复和告警链路回归

## 删除与未导入内容

| 内容 | 是否删除/不导入 | 原因 | 影响 |
| --- | --- | --- | --- |
| D1-D8 阶段报告及自检日志 | 是 | 历史过程产物，不参与构建和运行 | 不影响功能；提交历史保留实现依据 |
| `SelfCheck.java` | 是 | 约 125KB，位于生产源码且与测试目录检查重复 | 不进入生产包；由 JUnit 与回归测试替代 |
| `target` 构建目录 | 是 | 可重复生成的产物 | 无影响 |
| 历史 `run-*.sh` | 是 | 面向旧目录结构的阶段脚本 | 统一使用 `mvn test` |

## 验证命令

```text
mvn clean test
```

## 结果

- 编译生产源码：186 个文件
- 编译测试源码：32 个文件
- 自动化测试：119 个通过，0 失败，0 错误，0 跳过
- 在线监控协议：本地临时采集端验证 Run 开始、真实证据 Tool Call、Run 结束及采集密钥
- 自动评测关联：验证 `evaluationRunId` 进入真实在线监控 Run，隐藏标准答案不属于 Runtime 请求协议
- 提交编号：以本次 `main` 提交为准
